let incr = 10, //increment
    decr = 10; //decrement

console.log(++incr); //перфіксний обчислює, потім повертає
console.log(decr--); //постфіксний повертає старе число, потім обчислює

console.log(2*4 == 8); // порівняння
console.log(2*4 === 8); // суворе порівняння

// && "і" обидва = true
// || "або" один = true
// ! "знак заперечення"


const isChecker = true,
    isClose = false;

console.log(isChecker && !isClose);

